#include <bits/stdc++.h>

using namespace std;

def dfs(char** adj, int H, int W, int 


int main(){
	int H, W;
	cin >> H >> W;

	char[H][W] adj;

	for (int i = 0; i < H; i++){
		for (int j = 0 j < W; j++){
			char t;
			scanf("%c", &t);
			adj[i][j] = t;
		}
	}

	return 0;
}

